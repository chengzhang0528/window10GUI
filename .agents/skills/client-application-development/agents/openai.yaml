interface:
  display_name: "Client Application Development"
  short_description: "Start and verify client delivery with the smallest safe path"
  default_prompt: "Use $client-application-development to classify the client request, lock ownership and delivery boundaries, follow the fast path, and load only the needed platform or release reference."

policy:
  allow_implicit_invocation: true
