interface:
  display_name: "Solution Direction Review"
  short_description: "Align direction and reject unsupported policy logic"
  default_prompt: "Use $align-solution-direction to align outcome, boundary, invariants, ownership, and completion rules while rejecting checks or policy gates without an authoritative or user-facing need."

policy:
  allow_implicit_invocation: true
